{
  "name": "mxd-yaml-config-lib",
  "description": "Loads the given and existing .yml files",
  "version": "1.0.0",
  "homepage": "http://github.com/maxdome/yaml-config-lib",
  "license": "MIT",
  "readmeFilename": "README.md",
  "main": "src/lib.js",
  "scripts": {
    "lint": "eslint src/ test/",
    "test": "mocha --reporter spec",
    "test-cov": "istanbul cover node_modules/mocha/bin/_mocha -- --reporter dot"
  },
  "dependencies": {
    "yamljs": "^0.2.4"
  },
  "devDependencies": {
    "eslint": "^1.10.3",
    "eslint-config-airbnb": "^2.0.0",
    "istanbul": "^0.4.1",
    "mocha": "^2.3.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/maxdome/yaml-config-lib.git"
  }
}
